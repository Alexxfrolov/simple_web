$(window).on('ready resize load', function() {
    $('#js-wrapper').fullpage({
        sectionSelector: '.grid__section',
        scrollingSpeed: 600,
        navigation: true
    });
    $('#js-moveDown').on('click', function(event) {
        event.preventDefault();
        $.fn.fullpage.moveSectionDown();
    });
});